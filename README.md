# HTML Resume Layout (Pure HTML)

## 📄 Project Overview

This project is a simple resume / portfolio layout built using **pure HTML only** — no CSS, no JavaScript.

It demonstrates how a structured and readable layout can be created using only default browser styling and semantic HTML elements.

---

## ✨ Features

- Clean, minimal resume layout
- Built entirely with HTML
- No external libraries or frameworks
- Fully semantic structure
- Beginner-friendly example

---

## 🛠 Tech Stack

- **HTML5**
- Default browser styling (no CSS)

---

## 📚 Sections Included

The layout contains the following resume sections:

- Header (Name & Title)
- About Me
- Skills
- Experience
- Projects
- Education
- Contact Information
- Footer

---

## 🧱 HTML Elements Used

This project intentionally uses only core HTML elements:

- Headings (`h1`, `h2`, `h3`)
- Paragraphs (`p`)
- Lists (`ul`, `li`)
- Tables (`table`, `tr`, `td`, `th`)
- Horizontal rules (`hr`)
- Strong text (`strong`)

---


